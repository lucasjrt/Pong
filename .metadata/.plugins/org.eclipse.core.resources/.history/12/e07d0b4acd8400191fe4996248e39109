package br.facom.ufu.poo.objetos;

import java.awt.Color;
import java.awt.Graphics;
import java.awt.Rectangle;

import br.facom.ufu.poo.Coordenada;

public class Bloco {
	private Rectangle bloco;		// O que vai ser renderizado
	private final int playerId;	// ID do player, caso exista mais de um
	private Coordenada pos; 		// Centro do retangulo
	
	public Bloco(Rectangle bloco, Coordenada pos, int playerId) {
		this.playerId = playerId;
		this.pos = pos;
		this.bloco = bloco;
		System.out.println(pos.x);
		
		System.out.println(pos.x - bloco.x + ((bloco.width - bloco.x) >> 2));
		this.bloco.x = pos.x - (bloco.x + ((bloco.width - bloco.x) >> 2));
		this.bloco.y = pos.y - (bloco.y + ((bloco.height - bloco.y) >> 2));
		System.out.println(pos);
		System.out.println(bloco);
	}
	
	public void draw(Graphics g) {
		Color temp = g.getColor();
		g.fillRect(bloco.x, bloco.y, bloco.width, bloco.height);
		g.setColor(Color.blue);
		g.fillRect(pos.x, pos.y, 1, 1);
	}
	
	public void move(Coordenada c) {
		this.pos = c;
		this.bloco.x = c.x - (bloco.x + ((bloco.width - bloco.x) >> 2));
		this.bloco.y = c.y - (bloco.y + ((bloco.height - bloco.y) >> 2));
	}
	
	public int getX() {
		return pos.x;
	}
	
	public int getY() {
		return pos.y;
	}
	
	public int getPlayerId() {
		return playerId;
	}
	
}
