package br.facom.ufu.poo.objetos;

import java.awt.Color;
import java.awt.Graphics;
import java.awt.Rectangle;

import br.facom.ufu.poo.Coordenada;

public class Bola {
	private Rectangle bordas; // O circulo e renderizado como uma elipse, portanto precisa de um retangulo
	private int velocidade;
	private Coordenada aceleracao;
	private Coordenada pos;
	public Bola(int raio, int velocidade) {
		this.velocidade = velocidade;
		pos = new Coordenada(300, 300);
		aceleracao = new Coordenada();
		bordas = new Rectangle();
		bordas.x = pos.x - raio;
		bordas.y = pos.y - raio;
		bordas.width = 2 * raio;
		bordas.height = 2 * raio;
	}
	
	public synchronized void draw(Graphics g) {
		g.setColor(Color.white);
		g.fillRect(bordas.x + pos.x, bordas.y + pos.y, bordas.width, bordas.height);
		System.out.println("bola");
	}
	
	public void aplicaForca(int x, int y) {
		
	}
	
}
